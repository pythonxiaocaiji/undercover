# 快速开始指南

## 🎉 新功能已添加！

### 0. 快速游戏功能 ✅
**已完成并可用**

- 主界面新增"快速游戏"按钮（紫粉渐变色）
- 自动匹配进入允许快速匹配的大厅房间
- 创建房间时可配置"允许快速匹配玩家加入"
- 无需手动输入房间号，一键快速开始

**功能说明：**
1. 点击"快速游戏"按钮
2. 系统自动查找允许快速匹配的房间
3. 找到合适房间后自动加入
4. 如果没有可用房间，会提示"暂无可用房间"

**房主配置：**
1. 创建房间时，在"房间权限"中找到"允许快速匹配玩家加入"
2. 默认开启，可以关闭禁止快速匹配玩家加入
3. 关闭后，只能通过房间号或邀请加入

**测试方法：**
1. 房主创建房间，保持"允许快速匹配玩家加入"开启
2. 其他玩家点击"快速游戏"按钮
3. 应该自动加入房主创建的房间

### 1. 图片压缩功能 ✅
**已完成并可用**

- 上传头像时自动压缩
- 支持最大 10MB 的原始图片
- 自动压缩到 1MB 以内
- 无需额外配置

**测试方法：**
1. 进入个人设置页面
2. 选择一张大图片（2-10MB）
3. 系统会自动压缩并上传
4. 查看浏览器控制台可以看到压缩日志

### 2. 音效系统 ✅
**已完成，但需要添加音效文件**

#### 当前状态
- ✅ 音效管理器已实现
- ✅ 音效控制按钮已添加
- ✅ 音效触发点已设置
- ⚠️ 需要添加真实音效文件

#### 如何添加音效文件

**方法 1：使用免费音效网站**

1. 访问以下网站下载音效：
   - [Pixabay](https://pixabay.com/sound-effects/) - 推荐，无需注册
   - [Mixkit](https://mixkit.co/free-sound-effects/) - 推荐，无需注册
   - [Freesound](https://freesound.org/) - 需要注册
   - [Zapsplat](https://www.zapsplat.com/) - 需要注册

2. 下载以下 10 个音效文件：
   - `phase_change.mp3` - 阶段切换音效（轻快的过渡音）
   - `game_start.mp3` - 游戏开始音效（激动人心的开场）
   - `vote.mp3` - 投票音效（点击/确认音）
   - `eliminated.mp3` - 玩家淘汰音效（失败音）
   - `victory.mp3` - 胜利音效（欢快的胜利音乐）
   - `defeat.mp3` - 失败音效（失落音）
   - `player_join.mp3` - 玩家加入音效（提示音）
   - `player_leave.mp3` - 玩家离开音效（提示音）
   - `ready.mp3` - 准备音效（确认音）
   - `notification.mp3` - 通知音效（轻柔提示音）

3. 将文件放入 `public/sounds/` 目录

4. 重新构建项目：
   ```bash
   npm run build
   ```

**方法 2：使用 AI 生成音效**

可以使用 AI 工具生成音效：
- [ElevenLabs Sound Effects](https://elevenlabs.io/sound-effects)
- [Soundraw](https://soundraw.io/)

**方法 3：临时跳过（推荐用于快速测试）**

如果暂时没有音效文件，系统会自动跳过播放，不会影响游戏功能。

你可以先部署，后续再添加音效文件。

#### 测试音效系统

1. 打开游戏页面
2. 查看右上角的音效控制按钮（喇叭图标）
3. 点击可以切换静音/取消静音
4. 鼠标悬停可以调节音量
5. 玩一局游戏，观察各个阶段是否有音效

## 📦 部署到服务器

### 快速部署（推荐）

**在服务器上执行以下命令：**

```bash
# 进入项目目录
cd /opt/undercover

# 拉取最新代码
git pull

# 安装依赖
npm install

# 构建前端
npm run build

# 验证构建成功
ls -lh dist/assets/index-*.js
```

**然后在本地浏览器中：**
1. 按 `Ctrl + Shift + R` 硬刷新
2. 清除浏览器缓存（可选）
3. 重新访问网站

### 详细部署步骤

#### 1. 构建项目
```bash
npm run build
```

#### 2. 上传文件
将 `dist/` 目录的内容上传到服务器

#### 3. 添加音效文件（可选）
如果你已经准备好音效文件：
```bash
# 在服务器上创建目录
mkdir -p /path/to/your/website/sounds

# 上传音效文件
scp public/sounds/*.mp3 user@server:/path/to/your/website/sounds/
```

#### 4. 验证部署
1. 访问网站
2. 检查主界面是否显示"快速游戏"按钮
3. 测试图片上传（上传一张大图片）
4. 测试音效系统（点击右上角音效按钮）
5. 玩一局游戏，体验新功能

### 1. 构建项目
```bash
npm run build
```

### 2. 上传文件
将 `dist/` 目录的内容上传到服务器

### 3. 添加音效文件（可选）
如果你已经准备好音效文件：
```bash
# 在服务器上创建目录
mkdir -p /path/to/your/website/sounds

# 上传音效文件
scp public/sounds/*.mp3 user@server:/path/to/your/website/sounds/
```

### 4. 验证部署
1. 访问网站
2. 测试图片上传（上传一张大图片）
3. 测试音效系统（点击右上角音效按钮）
4. 玩一局游戏，体验新功能

## 🎮 功能演示

### 图片压缩演示
```
原始图片: 5.2 MB
↓ 自动压缩
压缩后: 0.8 MB
压缩比: 84.6%
上传成功！✅
```

### 音效触发时机
- 🎵 游戏开始 → `game_start.mp3`
- 🎵 阶段切换 → `phase_change.mp3`
- 🎵 投票 → `vote.mp3`
- 🎵 玩家淘汰 → `eliminated.mp3`
- 🎵 平民胜利 → `victory.mp3`
- 🎵 卧底胜利 → `defeat.mp3`

## 🔧 故障排除

### 问题1：图片压缩不工作
**解决方案：**
- 检查浏览器控制台是否有错误
- 确认 `browser-image-compression` 已安装
- 清除浏览器缓存

### 问题2：音效不播放
**可能原因：**
1. 音效文件不存在 → 添加音效文件到 `public/sounds/`
2. 浏览器自动播放策略 → 用户需要先与页面交互
3. 音效已静音 → 检查右上角音效按钮

**解决方案：**
- 打开浏览器控制台查看错误信息
- 确认音效文件路径正确
- 点击页面任意位置后再测试

### 问题3：音效控制按钮不显示
**解决方案：**
- 确认在游戏页面（不是首页）
- 检查 CSS 是否正确加载
- 刷新页面

## 📊 性能影响

### 图片压缩
- **CPU 使用**：压缩时会占用一些 CPU，但使用了 Web Worker，不会阻塞 UI
- **内存使用**：压缩大图片时会临时占用内存
- **上传速度**：由于文件变小，上传速度反而更快

### 音效系统
- **初始加载**：预加载所有音效文件（约 1-2MB）
- **内存使用**：音效文件会缓存在内存中
- **播放性能**：几乎无影响，使用原生 Audio API

## 🎯 下一步

1. **立即可做：**
   - ✅ 部署到服务器
   - ✅ 测试图片压缩功能
   - ⏳ 添加音效文件

2. **本周可做：**
   - 收集用户反馈
   - 优化音效文件大小
   - 添加更多音效触发点

3. **后续优化：**
   - 添加背景音乐
   - 音效主题切换
   - 更多压缩选项

## 💡 提示

- 图片压缩功能已经可以直接使用，无需额外配置
- 音效系统可以先不添加音效文件，不影响游戏功能
- 建议先部署测试图片压缩，音效文件可以后续慢慢添加
- 音效文件建议使用无版权或 CC0 许可的音效

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看浏览器控制台的错误信息
2. 检查 `DEPLOYMENT_CHECKLIST.md` 中的验证步骤
3. 参考 `public/sounds/README.md` 了解音效文件要求
