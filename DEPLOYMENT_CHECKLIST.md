# 部署验证清单

## 部署后验证步骤

### 1. 基础功能测试
- [ ] 访问网站，确认页面正常加载
- [ ] 检查手机端显示是否正常
- [ ] 测试登录/注册功能
- [ ] 检查浏览器控制台是否有错误

### 2. 新功能验证

#### 手机端界面优化
- [ ] 在手机浏览器打开网站
- [ ] 检查顶部栏显示是否正常（房间ID在小屏幕隐藏）
- [ ] 检查玩家卡片大小和布局
- [ ] 检查底部操作栏按钮大小
- [ ] 测试各个页面的响应式布局

#### 阶段转换动画
- [ ] 创建或加入房间
- [ ] 开始游戏，观察是否显示"发言阶段"动画
- [ ] 等待进入投票阶段，观察动画
- [ ] 查看投票结果时的动画效果
- [ ] 确认动画显示约2.5秒后自动消失

#### 图片上传修复
- [ ] 进入个人设置页面
- [ ] 尝试上传一个超过5MB的图片
  - 应该显示："图片过大，请选择小于5MB的图片"
- [ ] 尝试上传非图片格式文件（如PDF）
  - 应该显示："不支持的图片格式，仅支持 PNG/JPG/WebP"
- [ ] 上传一个正常的图片（<5MB，PNG/JPG/WebP）
  - 应该成功上传并显示新头像

### 3. 性能检查
- [ ] 检查页面加载速度
- [ ] 测试动画是否流畅
- [ ] 检查手机端滚动是否顺畅
- [ ] 观察内存使用情况（开发者工具）

### 4. 兼容性测试
- [ ] Chrome/Edge 浏览器
- [ ] Safari 浏览器（iOS）
- [ ] 微信内置浏览器
- [ ] 不同屏幕尺寸（手机、平板、桌面）

### 5. 后端日志检查
```bash
# 查看后端日志
tail -f /path/to/backend/logs/app.log

# 或者如果使用 systemd
journalctl -u your-backend-service -f
```

检查是否有：
- [ ] 错误日志
- [ ] 异常堆栈
- [ ] 性能警告

### 6. 数据库检查
- [ ] 确认用户头像路径正确保存
- [ ] 检查上传的文件是否存在于 `backend/uploads/avatars/` 目录
- [ ] 验证文件权限是否正确

## 常见问题排查

### 问题1：阶段转换动画不显示
**可能原因：**
- 前端代码未正确构建
- 浏览器缓存

**解决方案：**
```bash
# 清除浏览器缓存或强制刷新（Ctrl+Shift+R）
# 检查前端构建
cd /path/to/frontend
npm run build
```

### 问题2：图片上传仍然失败
**可能原因：**
- 后端 uploads 目录权限问题
- Nginx 配置问题

**解决方案：**
```bash
# 检查目录权限
ls -la backend/uploads/avatars/

# 设置正确权限
chmod 755 backend/uploads
chmod 755 backend/uploads/avatars

# 检查 Nginx 配置
nginx -t
```

### 问题3：手机端样式异常
**可能原因：**
- CSS 文件未更新
- 浏览器缓存

**解决方案：**
- 清除浏览器缓存
- 检查 CDN 缓存是否已刷新
- 验证构建产物中的 CSS 文件

## 回滚计划

如果发现严重问题需要回滚：

```bash
# 1. 回滚到上一个 commit
git revert HEAD
git push origin main

# 2. 或者回退到特定版本
git reset --hard <previous-commit-hash>
git push -f origin main

# 3. 重新部署
# 执行你的部署脚本
```

## 监控指标

部署后持续监控：
- [ ] 错误率是否增加
- [ ] 响应时间是否正常
- [ ] 用户反馈是否有新问题
- [ ] 服务器资源使用情况

## 用户通知

如果一切正常，可以通知用户：
- ✨ 优化了手机端显示效果
- ✨ 新增游戏阶段转换动画
- 🐛 修复了图片上传问题
- 📱 改进了移动端体验
